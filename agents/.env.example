# Arc testnet
ARC_TESTNET_RPC_URL=https://rpc.testnet.arc.network

# Funder / deployer key — pays gas and seeds the worker agents.
DEPLOYER_KEY=

# Autonomous agent keys. Generate with: npm run bootstrap -- --new-keys
# Each agent holds its own wallet and signs its own transactions.
BUYER_KEY=
WORKER_LEAK_KEY=
WORKER_SWIFT_KEY=
WORKER_FLAKY_KEY=
