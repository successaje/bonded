# Arc testnet (chain id 5042002). Fund the key at https://faucet.circle.com.
ARC_TESTNET_RPC_URL=https://rpc.testnet.arc.network

# Burner deploy key — NEVER commit the real .env, never reuse a mainnet key.
PRIVATE_KEY=

# Optional overrides (defaults: Arc testnet USDC, deployer as arbiter)
# USDC_ADDRESS=0x3600000000000000000000000000000000000000
# ARBITER=
